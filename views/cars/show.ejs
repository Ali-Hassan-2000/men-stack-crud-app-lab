<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= car.name %></title> <!-- Dynamic title based on car name -->
  </head>
  <body>
    <h1>Car name:<%= car.name %></h1>
    <h1>Car model:<%= car.carModel %></h1>
    <h1>Car color:<%= car.carColor %></h1>
    <h1>Car number:<%= car.carNumber %></h1>
    <h1>Car millage:<%= car.Millage %></h1>

    <form action="/cars/<%=car._id%>?_method=DELETE" method="POST">
      <button type="submit">Delete <%= car.name %></button>
    </form>

    <a href="/cars/<%= car._id %>/edit">Edit <%= car.name %></a>

    <a href="/cars">Back to Cars</a>
  </body>
</html>